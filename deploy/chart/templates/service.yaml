apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodevitals-observatory.fullname" . }}
  labels:
    {{- include "nodevitals-observatory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "nodevitals-observatory.selectorLabels" . | nindent 4 }}
